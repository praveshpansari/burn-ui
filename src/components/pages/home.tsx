import { Component } from 'solid-js';

const Home: Component = () => {
    return (
        <section class="bg-gray-50 dark:bg-primary-900">
            <div class="flex flex-col items-center justify-center px-6 mx-auto py-16">Home</div>
        </section>
    );
};

export default Home;
